import type { Config } from "tailwindcss";

const config: Config = {
	content: ["./src/**/*.{tsx}"],

	// TODO: GRID PATTERN, SPACES TEMPLATE PATTERN, FONT TEMPLATE PATTERNS
	theme: {
		extend: {},
	},
	plugins: [],
};
export default config;
